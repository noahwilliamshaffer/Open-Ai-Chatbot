{
  "name": "openai-api-integration",
  "version": "1.0.0",
  "description": "A project that integrates with OpenAI API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:cli": "node index.js",
    "docker:build": "docker build -t openai-chatbot .",
    "docker:run": "docker run -p 3000:3000 -e OPENAI_API_KEY openai-chatbot"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "openai": "^4.24.1"
  }
}
